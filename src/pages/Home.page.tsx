import ExpensesGroup from "components/expenses/ExpensesGroup";
import Layout from "helpers/Layout";

const Home = () => {
  return (
    <Layout>
      <ExpensesGroup />
    </Layout>
  );
};

export default Home;
